
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ACESSO TOTAL: Remove todos os bloqueios de segurança para garantir o funcionamento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
