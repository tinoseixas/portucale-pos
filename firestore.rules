
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permetre accés total a qualsevol usuari autenticat
    // Ja que el sistema està enfocat a administradors mestres
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
